---
name: Training run report
about: Report a failed/under-performing training run (block-diffusion SFT)
title: "[training] "
labels: training
assignees: ''
---

## Goal
- Model:
- Dataset:
- Config file (`configs/sft/*.yaml`):

## What happened
<!-- e.g. OOM, NaN loss, diverged, wrong checkpoint format after split. -->

## Hardware
- GPUs / count / VRAM:
- `data_parallel_mode` / `tensor_parallel_size`:

## Artifacts
- `log.txt` tail:
- Checkpoint path (`output_dir/checkpoints/global_step_XXX/hf_ckpt/`):
